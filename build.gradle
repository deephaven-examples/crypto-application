plugins {
    id 'com.diffplug.spotless' version '5.14.2' apply false
}

subprojects {
    apply plugin: 'com.diffplug.spotless'

    spotless {
        java {
            googleJavaFormat()
        }
    }
}